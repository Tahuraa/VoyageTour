package com.example.voyagetour

import io.flutter.embedding.android.FlutterFragmentActivity

// flutter_stripe's PaymentSheet needs a FragmentActivity host to present its
// native UI, so this can't be the default FlutterActivity.
class MainActivity : FlutterFragmentActivity()
